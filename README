README

Clustering pipeline

Required Arguments:
--matrix (file with delimited matrix)
--orientation (row or column) default column
--distance (distance function; default euclidean
	dCor, oneMinusdCor, dCov, oneMinusdCov, pearson, spearman, kendall 
	euclidean, maximum, manhattan, canberra, binary, minkowsky)

Execute
--print_distance (print distance matrix)
--hclust (perform hclust; (pearson, spearman, kendall) distances become 1 - cor before clustering)
--umap(apply umap disregarding distance function)

TODO
Allow multiple clustering methods (current is ward.D2)\
Allow umap with DBSCAn?
